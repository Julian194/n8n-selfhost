FROM docker.n8n.io/n8nio/n8n

USER root

# Install custom npm packages
RUN npm install -g inspirational-quotes
# Add any other npm packages you need here 

# Switch back to node user (the default n8n user)
USER node 